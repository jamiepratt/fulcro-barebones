shadow$provide.module$node_modules$victory_core$lib$victory_primitives$arc = function(global, require, module, exports) {
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.Arc = void 0;
  var _react = function(obj) {
    return obj && obj.__esModule ? obj : {default:obj};
  }(require("module$node_modules$react$index")), Helpers = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != typeof e && "function" != typeof e) {
      return {default:e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && Object.prototype.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n.default = e, r && r.set(e, n), n;
  }(require("module$node_modules$victory_core$lib$victory_util$helpers"));
  global = require("module$node_modules$victory_core$lib$victory_primitives$path");
  const defaultProps = {pathComponent:_react.default.createElement(global.Path, null), role:"presentation", shapeRendering:"auto"};
  exports.Arc = initialProps => {
    initialProps = {...defaultProps, ...initialProps};
    var ariaLabel = Helpers.evaluateProp(initialProps.ariaLabel, initialProps), desc = Helpers.evaluateProp(initialProps.desc, initialProps), id = Helpers.evaluateProp(initialProps.id, initialProps), style = Helpers.evaluateStyle(Object.assign({stroke:"black", fill:"none"}, initialProps.style), initialProps), tabIndex = Helpers.evaluateProp(initialProps.tabIndex, initialProps);
    initialProps = Object.assign({}, initialProps, {ariaLabel, desc, id, style, tabIndex});
    ariaLabel = _react.default;
    desc = ariaLabel.cloneElement;
    id = initialProps.pathComponent;
    style = {...initialProps.events};
    tabIndex = initialProps.ariaLabel;
    const {cx, cy, r, startAngle, endAngle, closedPath} = initialProps, halfAngle = Math.abs(endAngle - startAngle) / 2 + startAngle, x1 = cx + r * Math.cos(Helpers.degreesToRadians(startAngle)), y1 = cy - r * Math.sin(Helpers.degreesToRadians(startAngle)), x2 = cx + r * Math.cos(Helpers.degreesToRadians(halfAngle)), y2 = cy - r * Math.sin(Helpers.degreesToRadians(halfAngle)), x3 = cx + r * Math.cos(Helpers.degreesToRadians(endAngle)), y3 = cy - r * Math.sin(Helpers.degreesToRadians(endAngle));
    return desc.call(ariaLabel, id, {...style, "aria-label":tabIndex, d:`${closedPath ? ` M ${cx}, ${cy} L ${x1}, ${y1}` : `M ${x1}, ${y1}`} ${`A ${r}, ${r}, 0, ${180 >= halfAngle - startAngle ? 0 : 1}, 0, ${x2}, ${y2}`} ${`A ${r}, ${r}, 0, ${180 >= endAngle - halfAngle ? 0 : 1}, 0, ${x3}, ${y3}`} ${closedPath ? "Z" : ""}`, style:initialProps.style, desc:initialProps.desc, tabIndex:initialProps.tabIndex, className:initialProps.className, role:initialProps.role, shapeRendering:initialProps.shapeRendering, 
    transform:initialProps.transform, clipPath:initialProps.clipPath});
  };
};

//# sourceMappingURL=module$node_modules$victory_core$lib$victory_primitives$arc.js.map
