shadow$provide.module$node_modules$victory_vendor$lib_vendor$d3_color$src$cubehelix = function(global, require, module, exports) {
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  function cubehelix(h, s, l$jscomp$0, opacity) {
    if (1 === arguments.length) {
      var JSCompiler_temp = h;
      if (JSCompiler_temp instanceof Cubehelix) {
        JSCompiler_temp = new Cubehelix(JSCompiler_temp.h, JSCompiler_temp.s, JSCompiler_temp.l, JSCompiler_temp.opacity);
      } else {
        JSCompiler_temp instanceof _color.Rgb || (JSCompiler_temp = (0,_color.rgbConvert)(JSCompiler_temp));
        var g = JSCompiler_temp.g / 255, b = JSCompiler_temp.b / 255, l = (BC_DA * b + JSCompiler_temp.r / 255 * ED - EB * g) / (BC_DA + ED - EB);
        b -= l;
        var k = (E * (g - l) - C * b) / D;
        b = (g = Math.sqrt(k * k + b * b) / (E * l * (1 - l))) ? Math.atan2(k, b) * _math.degrees - 120 : NaN;
        JSCompiler_temp = new Cubehelix(0 > b ? b + 360 : b, g, l, JSCompiler_temp.opacity);
      }
    } else {
      JSCompiler_temp = new Cubehelix(h, s, l$jscomp$0, null == opacity ? 1 : opacity);
    }
    return JSCompiler_temp;
  }
  function Cubehelix(h, s, l, opacity) {
    this.h = +h;
    this.s = +s;
    this.l = +l;
    this.opacity = +opacity;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.Cubehelix = Cubehelix;
  exports.default = cubehelix;
  global = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != typeof e && "function" != typeof e) {
      return {default:e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && Object.prototype.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n.default = e, r && r.set(e, n), n;
  }(require("module$node_modules$victory_vendor$lib_vendor$d3_color$src$define"));
  var _color = require("module$node_modules$victory_vendor$lib_vendor$d3_color$src$color"), _math = require("module$node_modules$victory_vendor$lib_vendor$d3_color$src$math"), C = -0.29227, D = -0.90649, E = 1.97294, ED = E * D, EB = 1.78277 * E, BC_DA = 1.78277 * C - -0.14861 * D;
  (0,global.default)(Cubehelix, cubehelix, (0,global.extend)(_color.Color, {brighter(k) {
    k = null == k ? _color.brighter : Math.pow(_color.brighter, k);
    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
  }, darker(k) {
    k = null == k ? _color.darker : Math.pow(_color.darker, k);
    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
  }, rgb() {
    var h = isNaN(this.h) ? 0 : (this.h + 120) * _math.radians, l = +this.l, a = isNaN(this.s) ? 0 : this.s * l * (1 - l), cosh = Math.cos(h);
    h = Math.sin(h);
    return new _color.Rgb(255 * (l + a * (-0.14861 * cosh + 1.78277 * h)), 255 * (l + a * (C * cosh + D * h)), 255 * (l + a * E * cosh), this.opacity);
  }}));
};

//# sourceMappingURL=module$node_modules$victory_vendor$lib_vendor$d3_color$src$cubehelix.js.map
