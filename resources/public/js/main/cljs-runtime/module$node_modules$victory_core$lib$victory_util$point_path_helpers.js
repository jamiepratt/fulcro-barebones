shadow$provide.module$node_modules$victory_core$lib$victory_util$point_path_helpers = function(global, require, module, exports) {
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.circle = function(x, y, size) {
    return `M ${x}, ${y}
      m ${-size}, 0
      a ${size}, ${size} 0 1,0 ${2 * size},0
      a ${size}, ${size} 0 1,0 ${2 * -size},0`;
  };
  exports.cross = function(x, y, size) {
    size *= 0.8;
    const distance = size / 1.5;
    return `
      M ${x - distance / 2}, ${y + size + distance}
      v-${2 * distance}
      h-${distance}
      v-${distance}
      h${distance}
      v-${distance}
      h${distance}
      v${distance}
      h${distance}
      v${distance}
      h-${distance}
      v${2 * distance}
      z`;
  };
  exports.diamond = function(x, y, size) {
    size *= 0.87;
    size = Math.sqrt(2 * size * size);
    return `M ${x}, ${y + size}
      l ${size}, -${size}
      l -${size}, -${size}
      l -${size}, ${size}
      l ${size}, ${size}
      z`;
  };
  exports.minus = function(x, y, size) {
    const baseSize = 1.1 * size;
    size = baseSize - 0.3 * baseSize;
    const x0 = x - baseSize;
    x = x + baseSize - x0;
    return `M ${x0}, ${y + size / 2}
      h${x}
      v-${size}
      h-${x}
      z`;
  };
  exports.plus = function(x, y, size) {
    size *= 1.1;
    const distance = size / 1.5;
    return `
      M ${x - distance / 2}, ${y + size}
      v-${distance}
      h-${distance}
      v-${distance}
      h${distance}
      v-${distance}
      h${distance}
      v${distance}
      h${distance}
      v${distance}
      h-${distance}
      v${distance}
      z`;
  };
  exports.square = function(x, y, size) {
    size *= 0.87;
    const x0 = x - size;
    x = x + size - x0;
    return `M ${x0}, ${y + size}
      h${x}
      v-${x}
      h-${x}
      z`;
  };
  exports.star = function(x, y, size) {
    const baseSize = 1.35 * size, angle = Math.PI / 5;
    return `M ${Helpers.range(10).map(index => {
      const length = 0 === index % 2 ? baseSize : baseSize / 2;
      return `${length * Math.sin(angle * (index + 1)) + x},
        ${length * Math.cos(angle * (index + 1)) + y}`;
    }).join("L")} z`;
  };
  exports.triangleDown = function(x, y, size) {
    const y0 = y - size;
    return `M ${x - size}, ${y0}
      L ${x + size}, ${y0}
      L ${x}, ${y + size / 2 * Math.sqrt(3)}
      z`;
  };
  exports.triangleUp = function(x, y, size) {
    const y1 = y + size;
    return `M ${x - size}, ${y1}
      L ${x + size}, ${y1}
      L ${x}, ${y - size / 2 * Math.sqrt(3)}
      z`;
  };
  var Helpers = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != typeof e && "function" != typeof e) {
      return {default:e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && Object.prototype.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n.default = e, r && r.set(e, n), n;
  }(require("module$node_modules$victory_core$lib$victory_util$helpers"));
};

//# sourceMappingURL=module$node_modules$victory_core$lib$victory_util$point_path_helpers.js.map
